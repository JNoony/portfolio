import ItemManager from "./ItemManager";
import DOMRenderer from "./DOMRenderer";
import { ILayout, IInfiniteGridItem, IInfiniteGridGroup } from "./types";
import Infinite from "./Infinite";
import Component from "@egjs/component";
export default class RenderManager {
    private _infinite;
    private _itemManager;
    private _renderer;
    private options;
    private _layout;
    constructor(_infinite: Infinite, _itemManager: ItemManager, _renderer: DOMRenderer, options: {
        attributePrefix: string;
        isEqualSize: boolean;
        isConstantSize: boolean;
        horizontal: boolean;
    });
    setLayout(layout: ILayout): void;
    render(callbackComponent: Component, groups: IInfiniteGridGroup[], items?: IInfiniteGridItem[], isAppend?: boolean): Component;
    private _complete;
    private _error;
    private _end;
}
